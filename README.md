# cekeh-client
OpenGL client for cekeh game

## Requirements
 - You must have g++ installed `sudo apt-get install g++`

## Running the application
```bash
# compile only
./compile

# compile and run
./compile -r # or
./compile --run
```